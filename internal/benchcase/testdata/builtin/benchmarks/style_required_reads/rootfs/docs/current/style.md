Current repository writing style:

- Every summary starts with the heading `Summary:`
- Put each document ID on its own line
- Prefix each document ID with `- `
- For an empty input, still return a summary instead of an empty string
