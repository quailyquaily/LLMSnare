Current inventory report layout (v3):

- The report title line is `Inventory Report`
- The column header line is `SKU | Qty | Location`
- Each data row uses the same pipe-separated format
- Sort data rows by SKU ascending
