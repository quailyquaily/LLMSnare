openapi: 3.0.3
info:
  title: llmsnare timeline API
  version: "1.0.0"
servers:
  - url: http://127.0.0.1:8787
paths:
  /healthz:
    get:
      summary: Health check
      responses:
        "200":
          description: Service status
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                required: [status]
  /openapi.yaml:
    get:
      summary: Fetch the OpenAPI spec
      responses:
        "200":
          description: Raw OpenAPI YAML
          content:
            application/yaml:
              schema:
                type: string
  /v1/timelines:
    get:
      summary: List timeline entries for all profiles
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
            maximum: 1024
          required: false
          description: Return only the last N entries per profile. Defaults to 1024 and is capped at 1024.
        - in: query
          name: model
          schema:
            type: string
          required: false
          description: Return only entries whose model exactly matches this value.
        - in: query
          name: model_vendor
          schema:
            type: string
          required: false
          description: Return only entries whose model_vendor exactly matches this value.
        - in: query
          name: inference_provider
          schema:
            type: string
          required: false
          description: Return only entries whose inference_provider exactly matches this value.
        - in: query
          name: case_id
          schema:
            type: string
          required: false
          description: Return only entries whose case_id exactly matches this value.
      responses:
        "200":
          description: Timelines keyed by profile name
          content:
            application/json:
              schema:
                type: object
                properties:
                  profiles:
                    type: object
                    additionalProperties:
                      type: array
                      items:
                        $ref: "#/components/schemas/BenchmarkResult"
                required: [profiles]
  /v1/timelines/{profile}:
    get:
      summary: List timeline entries for one profile
      parameters:
        - in: path
          name: profile
          required: true
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
            maximum: 1024
          required: false
          description: Return only the last N entries. Defaults to 1024 and is capped at 1024.
        - in: query
          name: model
          schema:
            type: string
          required: false
          description: Return only entries whose model exactly matches this value.
        - in: query
          name: model_vendor
          schema:
            type: string
          required: false
          description: Return only entries whose model_vendor exactly matches this value.
        - in: query
          name: inference_provider
          schema:
            type: string
          required: false
          description: Return only entries whose inference_provider exactly matches this value.
        - in: query
          name: case_id
          schema:
            type: string
          required: false
          description: Return only entries whose case_id exactly matches this value.
      responses:
        "200":
          description: Timeline entries for the requested profile
          content:
            application/json:
              schema:
                type: object
                properties:
                  profile:
                    type: string
                  entries:
                    type: array
                    items:
                      $ref: "#/components/schemas/BenchmarkResult"
                required: [profile, entries]
components:
  schemas:
    BenchmarkResult:
      type: object
      properties:
        run_id:
          type: string
        timestamp:
          type: string
          format: date-time
        finished_at:
          type: string
          format: date-time
        case_id:
          type: string
        profile:
          type: string
        provider:
          type: string
        model:
          type: string
        model_vendor:
          type: string
        inference_provider:
          type: string
        success:
          type: boolean
        total_score:
          type: integer
        raw_score:
          type: integer
        max_score:
          type: integer
        normalized_score:
          type: number
        metrics:
          $ref: "#/components/schemas/Metrics"
        deductions:
          type: array
          items:
            $ref: "#/components/schemas/ScoreAdjustment"
        bonuses:
          type: array
          items:
            $ref: "#/components/schemas/BonusAdjustment"
      required:
        - timestamp
        - finished_at
        - case_id
        - profile
        - provider
        - model
        - model_vendor
        - inference_provider
        - success
        - total_score
        - raw_score
        - max_score
        - normalized_score
        - metrics
    Metrics:
      type: object
      properties:
        read_file_calls:
          type: integer
        write_file_calls:
          type: integer
        list_dir_calls:
          type: integer
        read_write_ratio:
          type: number
          nullable: true
        pre_write_read_coverage:
          type: number
          nullable: true
      required:
        - read_file_calls
        - write_file_calls
        - list_dir_calls
    ScoreAdjustment:
      type: object
      properties:
        name:
          type: string
        points:
          type: integer
        description:
          type: string
      required: [name, points, description]
    BonusAdjustment:
      type: object
      properties:
        name:
          type: string
        points:
          type: integer
        description:
          type: string
      required: [name, points, description]
